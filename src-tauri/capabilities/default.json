{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions — only the main window. Extend when adding plugins.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
